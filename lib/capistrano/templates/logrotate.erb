<%= fetch(:logrotate_log_path) %>/*.log {
    su <%= fetch(:logrotate_user) %> <%= fetch(:logrotate_group) %>
    <%= fetch(:logrotate_interval) %>
    rotate <%= fetch(:logrotate_logs_keep) %>
    missingok
    nocompress
    notifempty
    copytruncate
    dateext
    olddir <%= fetch(:logrotate_log_path) %>/archive
}
